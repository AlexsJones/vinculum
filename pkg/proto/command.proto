syntax = "proto3";
option go_package = "pkg/proto";
package proto;

service Command {
    rpc Send (Input) returns (stream Update) {
    }
}

message Input {
    string command = 1;
}

message Update {
    string error = 1;
}
